# Sekat akses terus ke folder ni dari web (defense-in-depth -
# folder ni sepatutnya sudah berada di luar document root public_html).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
